# 超星表单数据存储实测结论

测试日期：2026-08-13  
测试环境：武汉大学微服务后台、武汉大学珞珈在线智能体后台  
测试对象：`测试_生物实验智能体数据存储_20260813`  
超星表单 ID / 接口标识：`3513491`

## 一、结论

可以把智能体业务中的主要结果数据存到超星表单中，但需要区分两种实现方式：

1. 学生或老师直接打开超星表单填写：已经实测可用。
2. 高代码智能体在后台自动把每次问答、AI评价和报告写入超星：超星表单具备字段别名和接口标识，但仍需学校或超星提供表单数据接口文档与接口授权，当前尚未完成服务端接口联调。

因此，超星表单可以作为校内的数据承载和教师查看入口，但不能在没有接口授权的情况下，直接认定为智能体后端数据库已经全部打通。

## 二、本次实际完成的测试

已在超星中创建并发布独立测试表单，设置了以下字段：

| 业务数据 | 超星字段 | 字段别名 | 实测结果 |
|---|---|---|---|
| 学生唯一标识 | 单行文本 | `student_id` | 已创建、已提交 |
| 实验步骤编号 | 单行文本 | `step_id` | 已创建、已提交 |
| 回答版本 | 数字 | `version_no` | 已创建、已提交 |
| 学生实验描述 | 多行文本 | `student_answer` | 已创建、已提交 |
| AI反馈 | 多行文本 | `ai_feedback` | 已创建、已提交 |
| 是否允许进入下一步 | 单行文本 | `gate_status` | 已创建、已提交 |
| 综合评分 | 数字 | `total_score` | 已创建、已提交 |
| AI置信度 | 数字 | `ai_confidence` | 已创建、已提交 |
| 实验结果图片 | 图片 | `evidence_image` | 已上传并随记录提交 |
| 教师补充意见 | 多行文本 | `teacher_comment` | 已创建、已提交，可留空作为可选项 |
| 学习情况与改进报告 | 多行文本 | `final_report` | 已创建、已提交 |
| 超星账号归属 | 所属人 | 系统字段 | 已创建，用于账号身份归属 |

本次使用虚拟学生 `TEST_USER_001` 提交了一条 PAGE 实验描述测试记录，页面明确返回“提交成功”。实验图片使用老师已提供素材中的 `PCR鉴定结果.jpg`，上传完成后显示文件大小 16.87KB。

## 三、智能体后台联动测试

超星智能体后台的“资源管理—数据源—添加表单”已经能检索到本次新建表单：

- 表单 ID：3513491
- 表单名称：测试_生物实验智能体数据存储_20260813
- 创建人：姚佳
- 创建时间：2026-08-13 15:23:11

这证明微服务表单可以被智能体后台发现并添加为数据源。

本次没有把测试表单正式添加到现有“常见问题助手”，避免改变现有智能体配置。是否能够对这些记录进行稳定召回、按学生过滤以及实时更新，还需要在独立测试智能体中继续验证。

## 四、哪些内容可以放在超星

适合直接放在超星表单中的内容：

- 学生身份关联信息：超星用户、学号、班级、课程、实验任务。
- 每一步学生输入的实验文字描述。
- AI指出的遗漏、错误、风险点和补充要求。
- 每次修改后的版本号、时间、是否通过。
- 分项评分、总分和AI置信度。
- 学生上传的实验图片、结果图和附件。
- 教师可选的补充意见、复核状态。
- 最终学习情况和改进建议报告。

建议由高代码系统自己保留、再按需同步到超星的内容：

- 实时会话上下文、模型原始请求和响应。
- 提示词版本、模型版本、知识库命中文段和详细调试日志。
- 高频中间状态、流式输出和临时缓存。
- 复杂统计中间表、向量数据和需要事务一致性的关联数据。

## 五、还不能直接下结论的部分

以下内容目前只是看到平台能力或入口，尚未完成端到端验证：

1. 高代码后端自动新增、修改和查询表单记录的正式 API。
2. OAuth 登录后，超星用户 ID 与智能体业务学生 ID 的稳定映射。
3. 智能体数据源对新增记录的实时召回延迟。
4. 是否支持严格按当前学生过滤，避免召回其他学生数据。
5. 批量写入、并发上限、图片大小、单表数据量和接口限流。
6. 数据删除、修改、版本追溯和教师复核后的回写机制。

## 六、推荐落地方式

推荐采用“双层存储”：

- 智能体业务数据库保存完整过程数据，负责会话、版本、评分、状态机和权限控制。
- 超星表单保存学校需要查看和留痕的关键结果数据，负责身份依托、校内访问、教师查看和统计。

同步时机建议为：完成一次学生回答评价后写一条过程记录；完成一个实验或八步流程后再写一条汇总报告记录。教师补充意见保持可选，不阻塞学生立即得到AI反馈和改进报告。

## 七、需要向学校或超星确认的接口资料

1. 表单数据新增、查询、修改、删除接口文档。
2. 表单 ID、字段别名与接口请求字段的对应规则。
3. 应用 AppID、Secret、回调地址及接口授权范围。
4. 登录回调中可获得的用户 ID、学号、姓名、班级和课程字段。
5. 图片或附件上传接口及文件大小限制。
6. 单表容量、写入频率、查询频率和并发限制。
7. 智能体表单数据源的召回范围、权限过滤和更新延迟说明。
8. 测试环境或测试账号，避免直接在正式教学数据中联调。
