# 珞珈生化智能体 · UI 设计基线 V10

## 设计结论

唯一活动视觉方向是「珞珈山小实验家 · 科学探险手账」。界面服务于武汉大学生物化学文字实验，不模拟真实仪器操作；视觉可以有趣，但信息顺序、Gate 状态、证据边界和教师复核必须清楚严谨。

## 品牌与色彩

- 武汉大学标准校徽使用 `public/brand/wuhan-university-emblem.png`，来源为武汉大学官网“武大标识”下载包。
- 学习通使用项目已有 `public/brand/xuexitong-icon.jpg`，登录按钮与联合品牌标记复用同一文件。
- 标准主色：珞珈蓝 `#002554`、珞珈绿 `#115740`。
- 辅助色：珞樱粉 `#DD7187`、东湖蓝 `#5AA9D6`、秋桂黄 `#E7B44F`、EGFP 荧光绿 `#55D99A`。
- 背景以暖白 `#FFFAF2` 为主，深色仅用于明确的主操作与必要文字对比。

## 视觉语言

- 首页使用 AI 生成并压缩的明亮二维卡通实验场景，不含人物或性别指向；以 EGFP 荧光瓶、试管、细胞小助手、珞樱花和校园屋檐形成记忆点，HTML 只叠加课程标签与数据徽章。
- 内容页使用暖白实验记录纸、细线框和克制阴影，避免把长文本评阅做成娱乐化游戏。
- 学生端像“探险路线与实验记录册”，教师端像“同一本记录册的批阅工作台”。
- 品牌标记由 `BrandLockup` 统一输出；首页只显示武汉大学校徽，学习通图标只在登录按钮和需要说明平台身份的场景出现。
- 图标只使用 Lucide 或课程 SVG；不以 emoji 代替功能图标。装饰花朵不承担交互语义。

## 字体与信息层级

- 大标题：中文衬线字体栈，形成学术与校园气质。
- 正文与控件：中文无衬线字体栈，保证长文和移动端可读性。
- 状态、分数和步骤数字使用等宽数字特性，避免跳动。
- 每页只有一个主要动作；辅助资料、历史和完整评分依据保持二级层级。

## 动效规范

- 首页：实验卡整体悬浮，插画做明显的往返景深移动，叠加 EGFP 呼吸光晕、连续气泡、旋转分子轨道、扫光、飘落樱花和数据徽章错峰浮动；使用 CSS/GPU 动画，不加载视频或 GIF。
- 内容页：细胞轨道、实验气泡和路线虚线低速运动，不能干扰阅读和表单输入。
- 点击与悬停只做 1–2px 位移、描边或阴影变化，不让地图节点改变布局位置。
- `prefers-reduced-motion: reduce` 时停止所有装饰性动画；业务加载动画保留必要反馈。

## 响应式约束

- 手机基准：390×844，同时保证 360×800 不产生整页横向滚动。
- 所有主要触控目标至少 48×48px。
- 八步地图移动端保持单列；教师移动导航保持组件内单行横向滚动，禁止整页横向溢出。
- 桌面基准：1440×900；教师端只显示左侧一级导航，不重复显示移动导航。
- 单步任务的底部操作栏不得遮挡正文输入，阶段切换在 390px 内完整可见。

## 活动实现

- 主题令牌与页面覆写：`src/app/globals.css`、`src/app/ui-system.css`
- 联合品牌组件：`src/components/brand-lockup.tsx`
- 首页实验插画：`src/components/lab-hero-visual.tsx`
- AI 主视觉素材：`public/illustrations/biochem-hero-cartoon-v4.webp`
- 动态背景：`src/components/page-background.tsx`
- 入口、学生页头、教师页头分别复用上述组件，不保留另一套活动视觉系统。
